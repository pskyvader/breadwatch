const { createActivity } = require("./createActivity");
const { getActivity, getAllActivities } = require("./getActivity");
const { updateActivity } = require("./updateActivity");

module.exports = {
	createActivity,
	getActivity,
	getAllActivities,
	updateActivity,
};
