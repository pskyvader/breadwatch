const { login } = require("./login");
const { loginCookie } = require("./loginCookie");
const { logout } = require("./logout");
const { register } = require("./register");

module.exports = { login, loginCookie, logout, register };
